# Data and training artifacts (top-level only — src/flatjepa/data/ is source, not data)
/data/
/runs/
/checkpoints/
/wandb/
*.ckpt
*.pt
*.pth
*.npz
*.zarr

# Results under /outputs are intentionally NOT ignored: probe tables and figures are the
# reportable output of this project and should be versioned alongside the code that made them.
# Large binaries are excluded by the patterns above.

# Logs
/logs/
*.log

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.ipynb_checkpoints/

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp
